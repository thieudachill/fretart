import { defineConfig } from 'astro/config';

export default defineConfig({
  // The deployed URL — used for canonical + og:image absolute URLs.
  // Update when a custom domain is attached.
  site: 'https://fretart.nguyenthaithieu.workers.dev',
});
